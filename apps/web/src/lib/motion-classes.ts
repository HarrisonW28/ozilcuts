/** Theme tokens from `tailwind.config.ts` (`transitionDuration.brand`, `transitionTimingFunction.brand`). */
export const motionTransition = "duration-brand ease-brand" as const;

export const motionSafeTransition =
  "motion-safe:duration-brand motion-safe:ease-brand" as const;
