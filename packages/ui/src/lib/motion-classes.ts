/** Aligns with `transitionDuration.brand` / `transitionTimingFunction.brand` in the web Tailwind theme. */
export const uiMotionTransition =
  "motion-safe:duration-brand motion-safe:ease-brand" as const;

export const uiMotionTransitionPlain = "duration-brand ease-brand" as const;
